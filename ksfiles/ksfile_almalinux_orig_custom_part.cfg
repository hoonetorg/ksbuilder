# Generated by Anaconda 40.22.3.33
# Generated by pykickstart v3.52.9
#version=RHEL10
# Use graphical install
graphical

%addon com_redhat_kdump --enable --reserve-mb='auto'

%end

# Keyboard layouts
keyboard --vckeymap=us --xlayouts='us'
# System language
lang en_US.UTF-8

repo --name="AppStream" --baseurl=file:///run/install/repo/AppStream

%packages
@^workstation-product-environment

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.10.0
ignoredisk --only-use=sda
# Partition clearing information
clearpart --none --initlabel
# Disk partitioning information
part btrfs.83 --fstype="btrfs" --ondisk=sda --size=44454 --encrypted --luks-version=luks2
part /boot --fstype="xfs" --ondisk=sda --size=1024
part /boot/efi --fstype="efi" --ondisk=sda --size=600 --fsoptions="umask=0077,shortname=winnt"
part swap --fstype="swap" --ondisk=sda --size=5120 --encrypted --luks-version=luks2
btrfs none --label=almalinux btrfs.83
btrfs /home --subvol --name=home LABEL=almalinux
btrfs / --subvol --name=root LABEL=almalinux

# System timezone
timezone America/New_York --utc

#Root password
rootpw --lock
user --groups=wheel --name=hoo --password=$y$j9T$... --iscrypted --uid=1000 --gecos="hoo" --gid=1000
